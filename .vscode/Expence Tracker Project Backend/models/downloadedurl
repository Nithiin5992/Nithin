const Sequelize = require('sequelize');
const sequelize = require('../util/database');

const downloadedurl = sequelize.define('url', {
    id: {
        type: Sequelize.INTEGER,
        allowNull: false,
        primaryKey: true,
        autoIncrement: true
    },
    url: Sequelize.STRING,


});

module.exports = downloadedurl;